{
    "buildCommand": "prisma generate && next build",
    "installCommand": "npm install",
    "framework": "nextjs",
    "functions": {
      "api/**/*.ts": {
        "maxDuration": 60,
        "memory": 3008
      }
    },
    "crons": [
      {
        "path": "/api/cron/cerrar-asambleas-antiguas",
        "schedule": "0 0 * * *"
      }
    ]
  }